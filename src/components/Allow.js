import React, { Fragment } from 'react';
import './Allow.less';
import Settings from './Settings';

const Allow = () => {
    return (
        <Fragment>
            <div className="allow">
                <Settings />
            </div>
        </Fragment>
    );
}

export default Allow;
