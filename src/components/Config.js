import React, { Fragment } from 'react';
import './Config.less';
import Checkpoint from './Checkpoint';

const Config = () => {
    return (
        <Fragment>
            <Checkpoint />
        </Fragment>
    );
}

export default Config;
