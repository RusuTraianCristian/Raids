import React, { Fragment } from 'react';
import './Config.less';
import Settings from './Settings';

const Config = () => {
    return (
        <Fragment>
            <Settings />
        </Fragment>
    );
}

export default Config;
